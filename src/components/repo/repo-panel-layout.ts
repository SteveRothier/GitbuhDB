/** Carte pleine hauteur dans la grille 2 colonnes. */
export const REPO_CARD =
  "flex h-full min-h-[272px] flex-col rounded-xl border border-white/[0.06] bg-[#161b22] shadow-none overflow-hidden";

/** Zone liste / graphique extensible jusqu’en bas de la carte. */
export const REPO_PANEL_BODY = "flex min-h-0 flex-1 flex-col overflow-hidden";

export const RECENT_LIST_LIMIT = 5;
